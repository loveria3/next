# AI 확인 퀴즈 — 깃허브(GitHub Pages) 업로드 안내

`index.html` 파일 하나만 올리면 학생들과 링크로 함께 풀 수 있습니다. 설치할 것 없이 브라우저만 있으면 됩니다.

## 방법 A. 가장 쉬운 방법 (웹에서 클릭만)

1. github.com 로그인 → 오른쪽 위 **＋ → New repository**
2. Repository name에 `ai-quiz` 입력 → **Public** 선택 → **Create repository**
3. 새 저장소 화면에서 **uploading an existing file** 링크 클릭
4. `index.html` 파일을 화면에 **끌어다 놓기** → 아래 **Commit changes** 클릭
5. 상단 **Settings → 왼쪽 메뉴 Pages** 이동
6. Source에서 **Branch: main / (root)** 선택 → **Save**
7. 1~2분 뒤 페이지 상단에 뜨는 주소가 퀴즈 링크입니다:
   `https://<내아이디>.github.io/ai-quiz/`

## 방법 B. 링크를 QR로 만들기 (수업용)

- 위 주소를 네이버/구글에서 "QR코드 만들기"에 붙여넣으면 QR 이미지가 나옵니다.
- 슬라이드 12쪽에 QR을 띄우면 학생들이 각자 휴대폰으로 바로 접속할 수 있어요.

## 미리 확인하기 (업로드 전 테스트)

- `index.html`을 더블클릭하면 내 컴퓨터 브라우저에서 바로 열려 미리 풀어볼 수 있습니다.

## 문항 수정하고 싶다면

- `index.html`을 메모장/브라우저로 열어 위쪽 `const QUIZ = [ ... ]` 부분만 고치면 됩니다.
- 각 문제는 `q`(질문), `opts`(보기), `answer`(정답 번호, 0부터 시작) 로 되어 있습니다.
